# ── Datasets & databases — fetch separately, see data/README.md ─────────
data/**
!data/README.md

# ── Model weights / checkpoints / LoRA adapters ─────────────────────────
*.safetensors
*.bin
*.pt
*.gguf
checkpoints/
*_model/
lora_model/

# ── Result artifacts (regenerated by the notebooks) ─────────────────────
*.csv
*.jsonl
results/
outputs/

# ── Private build helper (references the author's Drive fileIds) ────────
_build/

# ── Notebook / Python cruft ─────────────────────────────────────────────
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/

# ── Secrets / environment ───────────────────────────────────────────────
.env
*.key

# ── OS ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
